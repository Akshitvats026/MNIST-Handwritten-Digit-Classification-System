# MNIST-Handwritten-Digit-Classification-System
Design and implement a basic image classification system using the MNIST handwritten digit dataset with the help of TensorFlow. The objective is to build, train, and evaluate a neural network model that can correctly classify grayscale images of handwritten digits (0–9) by learning patterns from the training data.
